#include "main.h"
#include <stdio.h>

/**
 * reverse_array - display a array in reverse
 * @a: string
 * @n: number of strings
 *
 */
void reverse_array(int *a, int n)
{
int x;
int i = 0;
while (i < n)
	i++;
for (x = (i - 1) ; a[x] >= 0 ;  x--)
{
}
}
