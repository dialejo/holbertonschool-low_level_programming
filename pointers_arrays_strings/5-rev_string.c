#include "main.h"
#include <stdio.h>

/**
 * rev_string - array to display the array
 *
 * @s: string
 *
 */

void rev_string(char *s)
{
pointer  = s[0]
int i;
int x = 0;
while (s[i] != '\0')
	i++;
for (x = 0 ; x < pointer ; x++)
{
	i--;
	pointer = s[x];
	s[x] = s[i];
	s[i] = pointer;
}
}
