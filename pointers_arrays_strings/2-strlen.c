#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - print the len of the array
 *
 * @s: string
 *
 * Return: the len of the array
 */

int _strlen(char *s)
{
int len;
len = strlen(s);
return (len);
}
