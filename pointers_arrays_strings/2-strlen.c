#include "main.h"
#include <stdio.h>

/**
 * int _strlen - print the len of the array
 *
 *@s : string
 *
 * Return the len of the array
 */

int int_strlen(char *s);
int len;
len = strlen(s);
_putchar(len);
