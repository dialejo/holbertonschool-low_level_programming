#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * *_strcpy - copy de pointer
 *
 * @dest: pointer
 * @src: string
 *
 * return: dest
 */

char *_strcpy(char *dest, char *src)
{
int i;
dest = src;
{
for (i = 0 ; src[i] <= '\0' ; i++)
}
return (dest);
}
